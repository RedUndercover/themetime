#!/bin/sh
printf '%s\n' 'ThemeTime installed. Enable the user scheduler with: systemctl --user enable --now themetime.service'
printf '%s\n' 'The privileged root daemon remains disabled until explicitly enabled.'
